apply plugin: "java"

sourceCompatibility = 1.6
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]


if (!hasProperty('mainClass')) {
    ext.mainClass = 'com.gannicus.desktop.DesktopLauncher'
}

eclipse.project {
    name = appName + "-core"
}
